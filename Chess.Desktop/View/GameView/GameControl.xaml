<UserControl x:Class="Chess.Desktop.View.GameView.GameControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Chess.Desktop.View.GameView"
             mc:Ignorable="d" 
             d:DesignHeight="750" d:DesignWidth="1100">
    <Grid Background="White">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="700"/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <ContentControl x:Name="boardContentControl" Grid.Column="0" Grid.Row="0">
            
        </ContentControl>
        <Border Grid.Row="0" Grid.Column="1" Background="MediumPurple"  Margin="20 20 20 40" CornerRadius="20">
            <Border.Effect>
                <DropShadowEffect Color="Gray" ShadowDepth="5"  />
            </Border.Effect>
            <Grid>
                
            </Grid>
        </Border>
    </Grid>
</UserControl>
